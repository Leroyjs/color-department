import { Text } from "components";
import { ButtonRectangle} from "components";
import { ModalShopWrapper } from "components";
import { H3 } from "components";
import { observer } from "mobx-react-lite";
import {
  ShopItemWrapper,
  ProductImage,
  ProductName,
  ProductPrice,
  TextContainer,
  TextStyled,
} from "./shop-item.style";

export const ModalShop = observer(({ cost, image, text }) => {
  return (
    <ModalShopWrapper>

    </ModalShopWrapper>
  );
});